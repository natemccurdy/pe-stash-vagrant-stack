---
roles:
  hosts:
    provisioners:
      - {type: hosts}

  2_cpus:
    providers:
      - type: virtualbox
        customize:
          - [modifyvm, !ruby/sym id, '--cpus', 2]

  auto_net:
    private_networks:
      - ip: '0.0.0.0'
        auto_network: true

  puppet_master:
    provisioners:
      - type: shell
        inline: "echo 'nameserver 8.8.8.8' > /etc/resolv.conf"
      - type: hosts
      - type: pe_bootstrap
        role: :master

  setup_control_repo:
    provisioners:
      - type: shell
        inline: "yes | cp /vagrant/environment.conf /etc/puppetlabs/code/environments/production/environment.conf"
      - type: shell
        inline: "yes | cp -r /vagrant/manifests /etc/puppetlabs/code/environments/production/"
      - type: shell
        inline: "yes | cp -r /vagrant/site /etc/puppetlabs/code/environments/production/"
      - type: shell
        inline: "yes | cp -r /vagrant/scripts /etc/puppetlabs/code/environments/production/"

  get_modules:
    provisioners:
      - type: shell
        inline: "/opt/puppetlabs/bin/puppet module install puppet-stash"
      - type: shell
        inline: "/opt/puppetlabs/bin/puppet module install puppetlabs-java"
      - type: shell
        inline: "/opt/puppetlabs/bin/puppet module install puppetlabs-postgresql"
      - type: shell
        inline: "/opt/puppetlabs/bin/puppet module install puppetlabs-java_ks"
      - type: shell
        inline: "/opt/puppetlabs/bin/puppet module install abrader-gms"
      - type: shell
        inline: "/opt/puppetlabs/bin/puppet module install pltraining-rbac"

  puppet_agent:
    provisioners:
      - {type: shell, inline: "echo 'nameserver 8.8.8.8' > /etc/resolv.conf"}
      - {type: hosts}
      - type: pe_bootstrap
        master: 'puppet-master'

  windows_base:
    communicator: "winrm"
    forwarded_ports:
      - { guest: 5985, host: 5986 }
      - { guest: 3389, host: 3390 }

  windows_agent_372:
    guest: windows
    private_networks:
      - {ip: '0.0.0.0', auto_network: true}
    provisioners:
      - type: pe_bootstrap
        version: '3.7.2'
        master: 'master372-centos'

  posix_agent:
    private_networks:
      - {ip: '0.0.0.0', auto_network: true}

  no_pe:
    provisioners:
      - {type: shell, inline: "echo 'nameserver 8.8.8.8' > /etc/resolv.conf"}
      - {type: hosts}

  centos:
    provisioners:
      - type: shell
        inline: 'service iptables stop; chkconfig iptables off;'
      - type: shell
        inline: '/usr/bin/yum install git tree telnet git -y'

  centos7:
    provisioners:
      - type: shell
        inline: '/usr/bin/yum install telnet vim tree git -y'
      - type: shell
        inline: 'systemctl stop firewalld.service; systemctl disable firewalld.service'

  customize_pe_via_hiera:
    provisioners:
      - type: shell
        inline: 'mkdir -p /var/lib/hiera; cp /vagrant/config/hierafiles/defaults.yaml /var/lib/hiera/defaults.yaml'



